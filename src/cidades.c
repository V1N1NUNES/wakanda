#include <stdio.h>
#include <stdlib.h>
#include "cidades.h"

//decalrações

//protótipos de funções


//funções
// Função que lê os dados do arquivo e inicializa a estrutura Estrada
Estrada *getEstrada(const char *nomeArquivo) {
    // Abrir arquivo, verificar erros e alocar memória para Estrada
    // Ler valores T e N, verificar restrições
    // Preencher Estrada com dados das cidades
}

// Função para calcular a menor vizinhança
double calcularMenorVizinhanca(const char *nomeArquivo) {
    // Calcular e retornar menor vizinhança entre as cidades
}

// Função para encontrar a cidade com a menor vizinhança
char *cidadeMenorVizinhanca(const char *nomeArquivo) {
    // Encontrar e retornar o nome da cidade com a menor vizinhança
}

